apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shopstream
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8082
  type: ClusterIP
